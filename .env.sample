### Sample .env for the Khoa_Luan RAG project
### Copy this file to .env and fill values before running the app

# OpenAI (optional). If set, UI can use OpenAI for embeddings/chat.
OPENAI_API_KEY=

# Local TEI (Text Embeddings Inference) base URL(s). If using docker-compose below, TEI services listen on ports 8800..8802
TEI_BASE_URL=http://localhost:8800

# Local chat service base URL (LocalAI). If using docker-compose below, LocalAI listens on 8080
LOCALAI_BASE_URL=http://localhost:8080/v1

# Default embedding model key (can be a TEI model key or a model folder name that build_index/GUI expects)
EMBEDDING_MODEL=

# Default chat model (for LocalAI). Can be left empty to auto-detect.
LOCALAI_CHAT_MODEL=
